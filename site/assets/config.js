// Site settings. Edit these before you deploy; empty values are hidden on the site.
window.FIRSTPRINT_CONFIG = {
  // Your public site address, e.g. "https://firstprint.xyz" (used in share links)
  siteUrl: '',
  // Social links shown in the footer and on "Coming soon" pages
  links: {
    x: '',
    telegram: '',
    discord: '',
  },
  // Optional waitlist form (Tally, Typeform, Google Forms). Adds "Join the waitlist" buttons.
  waitlistUrl: '',
};
